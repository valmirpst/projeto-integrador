export enum PerfilEnum {
  bibliotecario = 0,
  aluno = 1,
  professor = 2,
}

export enum CategoriaEnum {
  subcategoria = "subcategoria",
  categoria = "categoria",
}

export enum StatusEnum {
  ativo = "ativo",
  inativo = "inativo",
}
